<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/public/css/admin-dash.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <h1><%= user.username.charAt(0).toUpperCase() + user.username.slice(1) %></h1>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link btn btn-info" href="/createPost">Ny Post</a>
      </li>
      <li class="nav-item">
        <a class="nav-link btn btn-info" href="/logout">Logga ut</a>
      </li>
    </ul>
  </nav>

  <div class="row">
    <% if (posts.length > 0) { %>
      <% for (let i = posts.length - 1; i >= 0; i--) { %>
        <div class="card">
          <h2><%= posts[i].title %></h2>
          <h5><%= posts[i].posted.toLocaleString() %></h5>
          <div class="content-container">
            <div class="image-container">
              <img src="/uploads/<%= posts[i].image.toLowerCase() %>" alt="pic">
            </div>
            <div class="comment-container">
              <p><%= posts[i].comment %></p>
            </div>
          </div>
        </div>
      <% } %>
    <% } %>
  </div>
</body>
</html>
